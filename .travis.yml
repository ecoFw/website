#script:
# Rscript BUILD.R
#deploy:
#   provider: pages
#   repo: ecofw/ecofw.github.io
#   target_branh: master
#   local_dir: _site
#   skip_cleanup: true
#   github_token: $GITHUB_TOKEN # set in travis dashboard
#   fqdn: ecologicalfoodways.org
#   name: Travis

### FROM provtools
#before_install:
#  - wget https://github.com/gohugoio/hugo/releases/download/v0.27.1/hugo_0.27.1_Linux-64bit.deb
#  - sudo apt -y install ./hugo_0.27.1_Linux-64bit.deb
#script:
#  - hugo version
#  - hugo --ignoreCache=true
#deploy:
#  provider: pages
#  repo: ProvTools/provtools.github.io
#  target_branch: master
#  local_dir: public
#  skip_cleanup: true
#  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
#  fqdn: provtools.org
#  name: Travis
#notifications:
#  slack: endtoendprovenance:qqw61OZw9wGxFCX9mtotxJjH


### FROM mklau.info
# Install the apt prerequisites
# addons:
# apt:
#    packages:
#      - python-pygments
#
## Clean and don't fail
#install:
#  - rm -rf public || exit 0
#
## Build the website
# script:
#  - ../binaries/hugo --theme=minimo
#
## Deploy to GitHub pages
# deploy:
#  provider: pages
#  skip_cleanup: true
#  local_dir: public
#  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
#  on:
#    branch: master
